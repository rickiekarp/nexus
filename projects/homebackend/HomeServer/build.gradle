group 'net.rickiekarp.homeserver'

dependencies {
    compile project(':Foundation')
}

apply plugin: 'war'
build {
    war {
        archivesBaseName = 'HomeServer'
        libsDirName = new File(projectDir, "/../../module-deployment/service-tomcat/webapps")
        manifest {
            attributes 'Implementation-Version': new Date().format('yyMMddHHmm')
        }
    }
}