version: "3.4"

services:
  application:
    container_name: application
    build: service-application
    links:
      - database
    ports:
      - "8080:8080"
    volumes:
      - "./logs:/usr/local/tomcat/logs"
  database:
    container_name: database
    build: service-database
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=user
      - MYSQL_PASSWORD=1f3BcE47Fa91c4fe7df4
    ports:
      - "3306:3306"
